# このリポジトリでのClaudeの作業規則

## ブランチ

- `main`、`develop`、`feature/*`：Claudeによる直接コミットは禁止（`main`/`develop`はサーバー側でも強制）。
- `ClaudeWork/*`：Claudeが直接コミットできる唯一のブランチ。対象とする`feature/*`ブランチ（新規featureの場合は`develop`）から分岐する。

## 作業の昇格

- `ClaudeWork/*`から`feature/*`へのsquash mergeは、次のすべてを満たす場合にのみ行う：ユーザーが「確定」したことを確認済みである、本ファイルの規則を満たしている、diffに機密情報・認証情報が含まれない。

## Push

- Pushにはユーザーの明示的な許可が必要。許可は都度新たに得る。

## Pull Request

- PRの作成は可。PRのApproveおよびMergeは不可——その判断はユーザーに属する。

## 文書・ソースコードの書き方

- 規則・仕様・コメントは意味で判断できるよう宣言的に書き、手続き（手順1、手順2…）としては書かない。
- 履歴的な説明（何を・なぜ変更したか、過去に何があったか）はcommit messageにのみ書き、文書本文・コード（コメント含む）には「今どうあるべきか」だけを書く。
